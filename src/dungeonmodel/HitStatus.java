package dungeonmodel;

/**
 * The Hit Status of the Arrow.
 * Hit Status could be HIT if it hits the Otyugh, MISS if it misses or KILLED if Otyugh is killed.
 */
public enum HitStatus {
  HIT, MISS, KILLED
}
